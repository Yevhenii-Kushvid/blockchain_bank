<%= form_with(model: loan, local: true) do |form| %>
  <% if loan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(loan.errors.count, "error") %> prohibited this loan from being saved:</h2>

      <ul>
      <% loan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :loaner_id %>
    <%= form.text_field :loaner_id, id: :loan_loaner_id %>
  </div>

  <div class="field">
    <%= form.label :loanee_id %>
    <%= form.text_field :loanee_id, id: :loan_loanee_id %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount, id: :loan_amount %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.number_field :status, id: :loan_status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
